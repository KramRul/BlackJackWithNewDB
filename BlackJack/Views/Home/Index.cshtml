@{
    ViewData["Title"] = "Home Page";
}
<form>
    <div class="form-group">
        <label for="exampleFormControlSelect1">Выберите количество ботов</label>
        <select class="form-control" id="exampleFormControlSelect1">
            <option>1</option>
            <option>2</option>
            <option>3</option>
            <option>4</option>
            <option>5</option>
        </select>
    </div>
    <div class="form-group">
        <input name="city" list="cities" />

        <datalist id="cities">
            <option value="Naples" />
            <option value="London" />
            <option value="Berlin" />
            <option value="New York" />
            <option value="Frattamaggiore" />
        </datalist>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>
<div>
    @Html.RouteLink("Просмотреть всех игроков", new { action = "Index", controller = "User" })
</div>
<div>
    @Html.RouteLink("Просмотреть всех дилеров", new { action = "Index", controller = "Dealer" })
</div>
<div>
    @Html.RouteLink("Просмотреть все игры", new { action = "AllGames", controller = "User" })
</div>
<div>
    @Html.RouteLink("Register", new { action = "Register", controller = "User" })
</div>
<div>
    <button type="button" class="btn btn-primary btn-lg btn-block">Start Game</button>
</div>
